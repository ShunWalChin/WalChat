//  @ts-check
/** Formatação compartilhada por TypeScript, Markdown, JSON e configuração. */

/** @type {import('prettier').Config} */
const config = {
  semi: false,
  singleQuote: true,
  trailingComma: 'all',
}

export default config
